// Task 5 - Character Sequence

function Demo(input) {
  let text = input[0];

  for (let i = 0; i < text.length; i++) {
    let letter = text[i];
    console.log(letter);
  }
}

Demo(['softuni']);
Demo(['ice cream']);

/* for (let letter of text) {
   } 
 */
