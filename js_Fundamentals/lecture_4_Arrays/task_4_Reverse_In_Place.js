// Task 4 - Reverse in Place

function reverse(array) {
  array.reverse();
  console.log(array.join(' '));
}

reverse(['a', 'b', 'c', 'd', 'e']);
reverse(['abc', 'def', 'hig', 'klm', 'nop']);
reverse(['33', '123', '0', 'dd']);
