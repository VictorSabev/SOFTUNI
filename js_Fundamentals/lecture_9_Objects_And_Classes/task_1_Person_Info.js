// Task 1 - Person Info

function personalInfo(name, sirName, years) {
  let dataBase = {
    firstName: name,
    lastName: sirName,
    age: years,
  };

  return dataBase;
}
  
personalInfo('Peter', 'Pan', '20');
personalInfo('George', 'Smith', '18');
